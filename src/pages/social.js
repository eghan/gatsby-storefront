import React from 'react'
import { Link } from 'gatsby'
import styled from 'styled-components'

import Layout from '../components/layout'

const SocialDiv = styled.div`
    padding: 30px;
`

const About = () => (
  <Layout>
    <SocialDiv>
    Links to social spaces
    </SocialDiv>
  </Layout>
)

export default About
